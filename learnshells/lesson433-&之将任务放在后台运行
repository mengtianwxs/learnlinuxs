command & 将一个命令或脚本放到后台执行后,会显示这个后台任务的编号及其对应的子进程号.
jobs -l 使用-l选项,jobs命令可以显示后台正在运行的任务的进程号
将后台任务放到前台来运行
%job-id 任务编号job-id 或 fg job-id
fg命令用于把指定的任务放在前台运行,并把它作为当前运行的任务.
sleep 30 &
[1] 11287
%1
fg 1
如何在将前台的任务重新放回后台运行
ctrl + z 先将前台的任务挂起,然后在命令行提示符下输入如下命令
%1 &
或　bg
bg命令用于将挂起的任务放在后台继续运行.




